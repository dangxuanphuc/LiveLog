<% provide :title, "Add member" %>
<% provide :button_text, "Sign up" %>
<header class="page-header">
  <h2>Add Member</h2>
</header>
<%= form_for(@user, html: {class: "form-horizontal"}) do |f| %>
  <%= render "shared/error_messages", object: f.object %>
  <%= render "form", f: f, full: false %>
<% end %>
