<% if can_edit? song %>
  <%= link_to glyphicon(:edit), edit_song_path(song) %>
<% end %>
<% if logged_in? && current_user.played?(song) %>
  <% if song.open? %>
    <%= glyphicon(:globe) %>
  <% elsif song.secret? %>
    <%= glyphicon(:lock) %>
  <% end %>
<% end %>
